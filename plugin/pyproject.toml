[project]
name = "prompt-engineer"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.45",
    "openai>=1.0",
    "tiktoken>=0.7",
]

[tool.setuptools.packages.find]
include = ["lib", "lib.*"]
